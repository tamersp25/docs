{
  "version": 2,
  "build": {
    "env": {
      "ENVIRONMENT": "now"
    }
  },
  "builds": [{
    "src": "package.json",
    "use": "@now/static-build",
    "config": {
      "distDir": "build-now"
    }
  }]
}
